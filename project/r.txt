python predict.py \
  --input_dir test_pdbs \
  --output_dir results \
  --model_dir data/deepfri_models/trained_models \
  --ontologies mf cc bp


No upimapi colocar oçao de descarregar apenas a base de dados q queremos, nao dá tentar procurar soluções alternativas
apre: 


snakemake --use-conda --cores 10 --conda-frontend conda



ENV_PATH=/home/b_neil/projeto/recognizer_project/.snakemake/conda/ec6145ed65caee367604635351c6a317_
source $ENV_PATH/bin/activate || conda activate $ENV_PATH
LIBIOMP_PATH=$(find $ENV_PATH/lib -name "libiomp5.so" | head -n 1)
LD_PRELOAD=$LIBIOMP_PATH python -c "import torch; print(torch.__version__)"